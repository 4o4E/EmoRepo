# AGENTS.md

## 文档优先

- 修改代码前先阅读 `docs/README.md`、`docs/traceability.md` 和与任务直接相关的专题文档。
- 用户可观察行为、协议、同步、冲突、存储、安全、IPC、QAux 生命周期和错误恢复必须先形成文档并由用户确认。
- 文档状态不是 `confirmed` 时，不得实现相应行为。
- 如果实现需要文档未覆盖的逻辑，停止该部分工作并向用户确认，不得自行补默认行为。
- 已确认文档与代码冲突时，以文档为准；若需改变文档，先说明影响并重新确认。
- 完成实现和测试后更新 `docs/traceability.md`，不得只改代码不更新状态。

## 可以自行决定的实现细节

- 不改变外部行为的私有命名、等价 Kotlin 写法、局部重构和测试辅助代码可以自行决定。
- 一旦实现细节影响数据、兼容性、网络请求、权限、性能边界或错误结果，就必须文档化并确认。

## 项目边界

- EmoRepo 业务代码使用 Kotlin 和 Gradle Kotlin DSL。
- QAuxiliary PR 中面向外部模块的稳定 SPI 可以使用少量 Java；不得把这一例外扩展到 EmoRepo 业务代码。
- EmoRepo 不直接定位或 Hook QQ 类，只支持 Android QQ，不支持 TIM。
- 不为旧 `index.json` 实现读取、迁移或双写。
- 不复制 QAuxiliary 的 Hook/定位实现到本仓库。
- 不创建没有当前用途的模块、抽象或文档。

## 验证

- 源码检查、JVM 测试、Android 构建、QAux 集成和 QQ 真机测试必须明确区分。
- 未在真实 QQ 进程验证的功能，不得描述为运行时已通过。
