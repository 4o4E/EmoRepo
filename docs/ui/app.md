# App UI

- 状态：`confirmed`
- 更新：2026-08-31

## 技术

- 使用 Kotlin 和 Jetpack Compose Material 3。
- 第一版只适配 Android QQ 使用场景，不提供 TIM 入口。

## 页面

App 只保留一条层级导航：表情包列表为主页，短按表情包进入包内表情列表，设置是可返回的子页面。不再提供底部导航或独立“添加表情”路由。

- 主页左上角使用三横线按钮进入软件设置；该按钮是普通导航入口，不改变后续返回和页面状态。
- 设置页和表情包内容页都提供左上角返回，系统返回遵循同一层级。
- QQ 面板快捷入口按普通方式打开主页，不建立特殊设置路由。

首次启动且尚未完成仓库配置时，不显示主导航，进入三步引导：

1. `仓库`：填写不含用户名、密码和查询参数的 HTTPS 远端；Token 对公开仓库可留空。
2. `身份和设备`：填写 Git 作者名、邮箱；设备 ID 使用自动生成值并允许修改。
3. `克隆`：汇总非敏感配置并开始克隆，显示进度和可读错误，不允许重复发起。

- 引导字段在开始克隆前全部校验；Token 不在汇总页回显。
- 克隆先写同级临时目录，成功后才切换为正式私有仓库；失败不得留下“已配置”状态。
- 克隆成功后进入主界面并立即加载表情包；后续启动直接进入主界面并请求同步。
- 已完成配置后，主界面显示表情包、最近同步状态、未推送状态、手动同步和设置入口。

仓库页进入表情管理页。设置页中的每一项必须接入实际行为，不保留仅保存但不生效的占位项：

- 仓库：显示脱敏 HTTPS 远端、同步状态、最近成功时间、最近错误和手动同步。
- 仓库空间：显示工作区大小、Git 大小、完整/浅历史状态和最近维护结果；“立即优化仓库”在后台先同步，再按安全前置条件把 Android 本地历史收缩到最近 5 个提交。该操作不修改远端历史。
- Git 身份：编辑作者名和邮箱。
- 凭据：输入新 Token 后替换 Keystore 中的旧 Token并立即请求同步；页面不回显现有 Token。
- 同步：后台远端轮询间隔，`0` 表示关闭，其他值不得低于 15 分钟；使用记录同步延迟允许 `0`。
- 使用记录：设备 ID 和本设备最大记录数；修改设备 ID 按协议合并改名，修改数量后立即裁剪本设备记录。
- QQ 面板：设置每行表情数量，默认 4，只允许 3–8；保存后由 QQ 下次打开面板时读取，单元格高度随列数自动调整。
- 提交：编辑非空提交信息，后续自动提交使用新值。
- 诊断：通过系统文件创建器导出脱敏诊断 ZIP；明确提示不包含 QQ 聊天内容、Token、表情和索引。
- 软件更新：显示当前版本，匿名检查 public GitHub 最新 Release；发现新版本后按设备 ABI 下载、校验并进入系统安装确认。详细安全和失败行为见 `release/in-app-updates.md`。

APK 签名证书摘要不属于当前 App 使用设置，不在首次引导或设置页展示。

## 表情包列表

- 支持列表和平铺两种布局，并保存用户的布局选择。
- 列表/平铺使用紧凑的 Material 3 单选分段按钮组，只显示列表和网格图标；图标必须提供可访问说明。
- 主页内容区第一行左侧只显示 `共 n 个表情包`，右侧显示紧凑布局按钮组；主页不汇总或逐包显示表情数量，数量只在对应详情页显示。
- 平铺和列表的第一个项目固定为“+ 新建表情包”，点击打开名称确认框；它不是根索引记录，也不参与数量、排序或折叠。
- 平铺模式固定为一行四个表情包，不按屏幕宽度变成两列或三列。
- 两种布局都只显示表情包封面和完整名称，不在主页卡片显示表情数量。名称不得因数量占位而省略；空间不足时允许换行并增加卡片信息区高度。
- 平铺卡片的封面减少外边距，图片下方使用深色信息条显示完整名称，不再用大面积卡片边框包围内容。
- 显式 `icon` 记录是封面；没有显式封面时使用 JSONL 第一条有效表情；空包显示占位图。
- 封面是 GIF 时自动循环播放，并按封面区域裁切填满。
- `collapsed: true` 的表情包默认收进列表末尾的单一虚拟表情包；字段缺失的普通包显示在前。虚拟表情包名称固定为“折叠”，使用归档图标代替真实表情封面，不显示折叠数量，也不写入根索引。
- 点击“折叠”后，在它后面按根索引行序直接显示所有已折叠表情包；再次点击同一个“折叠”入口切回收起状态。列表布局中它与普通列表卡片同级，四列平铺中它只占一个网格单元，不使用全宽标题或嵌套滚动容器。
- 展开和收起使用稳定表情包 key 并保留当前 Lazy 列表锚点，不回到顶部，也不影响表情包内容页各自的滚动状态；重新进入页面时默认收起。排序编辑状态仍直接显示全部真实表情包，不显示或持久化这个虚拟入口。
- 主页长按表情包弹出操作菜单，提供“重命名”“删除”“折叠/取消折叠”“进入编辑”。折叠切换可逆，点击后立即持久化；重命名和删除继续二次确认，进入编辑后显示全部包并允许拖动排序和批量调整折叠状态。
- 进入排序模式后，后续调整不再要求重复长按；按住卡片直接拖动即可。
- 排序模式显示全部表情包，每张卡片提供明确的“折叠/取消折叠”操作；只有用户点击该操作才改变折叠状态，新建、导入和同步都不能自动折叠。
- 排序模式在内容首行明确显示 `取消` 和 `完成`，并暂时隐藏布局切换；普通短按不再进入表情包。
- 拖动和松手只更新内存中的草稿顺序，不写仓库、不触发同步；可以连续调整多个表情包。
- 点击 `完成` 才调用仓库层一次性写入顺序和折叠草稿，并按表情修改同步策略提交任务。
- 点击 `取消` 或在排序模式按系统返回键会丢弃草稿并恢复进入排序前的顺序。
- 长按开始和拖动结束提供触觉反馈，拖动项使用浮起效果；普通状态短按仍进入表情包，不增加常驻拖动按钮。
- QQ 通过 Provider 成功新增至少一张表情后，Provider 必须发布仓库内容变化；已创建的 App 页面在重新回到前台时重新读取仓库，因此无需杀死 App 或手动同步即可看到新增表情。重复项或全部失败不发布伪变化。

## 表情包管理

- 表情网格固定为四列，不随屏幕宽度改成三列或五列。
- 内容页标题区域显示完整表情包名称，并在名称下方显示 `共 n 个表情`；主页不重复显示该数量。
- 普通模式第一格固定为加号按钮，点击直接打开支持多选的系统图片选择器；选择返回后继续执行通用二次确认。加号不是表情记录，不参与预览和顺序。
- 内容页右上角使用文字“编辑”；点击后进入草稿多选编辑状态，右上角改为“完成”，第一格加号隐藏。
- 普通模式下，表情项只显示图片预览，不显示常驻容器背景、内边距或 `png`、`jpg`、`gif` 等格式文本；点击图片打开大图预览。
- 多选状态使用选中边框和多选框表达，不恢复每项常驻容器底色。
- GIF 在表情卡片和大图预览中自动循环播放，其他图片继续使用带插值过滤的静态预览。
- 普通模式不显示多选框。编辑状态全部卡片显示多选框，点击卡片或多选框切换选择，并提供“删除”和“移动到开头”。
- 删除只从编辑草稿中移除目标；移动到开头按当前视觉顺序把所选项整体移到首行之前。两种操作都不立即写文件、不触发同步。
- 点击“完成”才一次性写入最终行序并删除草稿中移除的图片，随后更新本设备最近使用并触发一次同步；系统返回或顶部返回直接丢弃全部草稿，不修改文件。
- 编辑状态保留从多选框长按后拖动连续选择；普通纵向滚动在长按成立前不被拦截。

## 导入确认

- 系统文件选择器使用多选模式；页面不额外解释多选和二次确认等通用交互。
- 文件选择器返回后不立即写仓库，先显示当前表情包和选中数量；用户二次确认后才开始导入。
- 取消确认不导入任何图片，也不触发 Git 同步。

## 新建反馈

- 点击“新建表情包”后名称输入框自动获得焦点并调起输入法。
- 创建成功提示只显示约 `1.5` 秒；提示到期前若出现新的消息，不得误清除新消息。

## 2026-09-01 折叠入口和 QQ 导入刷新验收

- `CPH2609` 真机的列表和平铺布局都已将折叠区显示为普通尺寸的虚拟“折叠”表情包，封面为归档图标；点击后真实折叠包紧跟在它后面显示，再次点击恢复收起。
- 在 QQ 群聊“洛”中向“测试折叠”导入一张不同图片后，包内 `index.jsonl` 从 1 行变为 2 行；App 进程 PID 保持不变，并记录 `provider/import_finished`、`ui_state/repository_change_applied`，回到原 App 任务后详情直接显示“共 2 个表情”。
- 本轮只使用已有图片消息的“添加到 EmoRepo”菜单，没有点击或发送任何表情，也没有向其他聊天发送消息。

## 全屏预览

- 从表情包内点击任意表情后进入全屏预览，并定位到被点击项。
- 预览按当前 JSONL 行序组成水平分页，未放大时左右滑动切换上一张或下一张。
- 当前页支持双指缩放、双击缩放、放大后单指拖动和惯性；缩放到边缘后允许继续切换相邻页。
- 切换页面后，每一页独立维护缩放状态；关闭预览后不保留该状态。
- GIF 在全屏预览中继续自动循环播放。
- 顶部显示关闭入口和 `当前页 / 总数`；系统返回键关闭预览。
- 长按当前预览打开操作菜单，提供 `导出` 和 `转发`。
- `导出` 使用系统文件保存位置选择器，以原始文件名和原始字节写出；取消位置选择不产生文件。
- `转发` 使用 Android 系统 Sharesheet 分享原文件，只对接收应用授予该文件的临时只读权限，不暴露仓库路径或其他文件。

## 首版边界

- 只管理一个仓库并跟踪远端默认分支。
- 不提供多仓库切换、SSH、公共目录直接管理或本地空仓库初始化。
- 第一阶段错误只在 App 内显示，不申请系统通知权限。
