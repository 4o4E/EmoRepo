# 数据格式

- 状态：`confirmed`
- 日期：2026-08-25

## 决策

- 保持当前表情包目录和记录字段语义。
- `index.json` 改为逐行对象的 `index.jsonl`。
- 增加 `order`，所有最终排序落到该字段。
- 最近使用保持每设备 CSV。
- 不实现旧 `index.json` 兼容。

## 后果

格式测试只能在正式仓库复制件上进行。字段校验、序列化和冲突算法以 `docs/protocol` 为准。
