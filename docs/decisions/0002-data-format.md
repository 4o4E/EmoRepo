# 数据格式

- 状态：`confirmed`
- 日期：2026-08-30

## 决策

- 保持当前表情包目录和记录字段语义。
- `index.json` 改为逐行对象的 `index.jsonl`。
- 根索引和包内索引都以 JSONL 行序表达最终顺序，不保存 `order` 字段。
- 新表情插入包内索引首行；调整顺序只移动完整记录行。
- 已发布的 `order` 字段文件首次读取时按旧值恢复可见顺序并原子迁移。
- 最近使用保持每设备 CSV。
- 不实现旧 `index.json` 兼容。

## 后果

格式测试只能在正式仓库复制件上进行。字段校验、序列化和冲突算法以 `docs/protocol` 为准。
